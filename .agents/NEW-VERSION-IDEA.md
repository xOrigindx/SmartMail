# Future Version / Long Term Roadmap

- [ ] **Custom Category Maker:** Build a UI that allows users to create their own custom item categories by dragging and dropping items, manually typing item IDs, or querying PeriodicTable sets, so they don't have to be hardcoded.
- [ ] **Out of the box thinking:** Implement invisible, machine-readable data strings (JSON/serialization) in the mail body for profile syncing, bank alt inventory manifests, or remote item requests.
